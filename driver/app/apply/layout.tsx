import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Candidature livreur',
}

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children
}

