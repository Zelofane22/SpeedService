import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Documents complémentaires',
}

export default function ApplicationComplementLayout({ children }: { children: React.ReactNode }) {
  return children
}

